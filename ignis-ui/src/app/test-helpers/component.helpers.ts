import { Component } from "@angular/core";

/**
 * Shell component used for component integration tests.
 * Replace the template with any components you need
 * to test.
 */
@Component({
  selector: "app-shell-component",
  template: "<div>Replace me</div>"
})
export class ShellComponent {}
