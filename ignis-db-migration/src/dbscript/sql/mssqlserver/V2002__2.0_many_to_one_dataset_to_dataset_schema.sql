ALTER TABLE DATASET
	ADD DATASET_SCHEMA_ID BIGINT
	CONSTRAINT DATASET_SCHEMA_FK FOREIGN KEY (DATASET_SCHEMA_ID) REFERENCES DATASET_SCHEMA(ID);

GO

UPDATE DATASET
  SET DATASET_SCHEMA_ID = (
		SELECT ID
		FROM DATASET_SCHEMA
		WHERE DATASET_SCHEMA.NAME = DATASET.TABLE_NAME
	);